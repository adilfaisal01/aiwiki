services:
  aiwiki:
    build: .
    ports:
      - "8000:8000"
    environment:
      - AIWIKI_LLM_PROVIDER=simulated
      - AIWIKI_AGENT_CYCLE_INTERVAL=300
    volumes:
      - aiwiki_data:/app/data

volumes:
  aiwiki_data:
